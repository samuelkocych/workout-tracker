<Window x:Class="WorkoutTrackerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorkoutTrackerApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1200"
        MinHeight="600" MinWidth="900"
        MaxHeight="1000" MaxWidth="1500"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- sidebar -->
        <StackPanel Background="{DynamicResource MaterialDesignPape}" Width="220">
            <Button Name="btnHome" Content="🏠 HOME" Style="{StaticResource MaterialDesignFlatButton}" Click="btnHome_Click"/>
            <Button Name="btnNewTraining" Content="➕ NEW TRAINING" Style="{StaticResource MaterialDesignFlatButton}" Click="btnNewTraining_Click"/>
            <Button Name="btnHistory" Content="📜 HISTORY" Style="{StaticResource MaterialDesignFlatButton}" Click="btnHistory_Click"/>
            <Button Name="btnExercises" Content="💪 EXERCISES" Style="{StaticResource MaterialDesignFlatButton}" Click="btnExercises_Click"/>
        </StackPanel>

        <!-- main content -->
        <Frame Name="mainFrame" Grid.Column="1" Source="Home.xaml" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
